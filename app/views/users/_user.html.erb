<li class="d-flex mb-1">
  <%= image_tag user.avatar_url, width: 40, height: 40, class: "img-thumbnail me-1" %>
  <p class="mb-0">
    <%= link_to "#{user.name}", user_path(user), class: "no-decoration d-block"%>
    <span><%= user.username %></span>
  </p>
</li>